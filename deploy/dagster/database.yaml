# ==============================================================================
# Dagster PostgreSQL Database (CloudNativePG)
# ==============================================================================

apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: dagster-db
  namespace: lotus-lake
spec:
  instances: 1

  imageName: ghcr.io/cloudnative-pg/postgresql:16.2

  affinity:
    nodeSelector:
      storage.h-kube.io/bulk: "true"

  storage:
    size: 5Gi
    storageClass: local-path

  bootstrap:
    initdb:
      database: dagster
      owner: dagster

  backup:
    barmanObjectStore:
      destinationPath: s3://cnpg-backups/lotus-lake-dagster
      endpointURL: http://seaweedfs-s3.seaweedfs.svc.cluster.local:8333
      s3Credentials:
        accessKeyId:
          name: dagster-db-backup-creds
          key: ACCESS_KEY_ID
        secretAccessKey:
          name: dagster-db-backup-creds
          key: SECRET_ACCESS_KEY
    retentionPolicy: "7d"

---
# ==============================================================================
# Scheduled Backup
# ==============================================================================

apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: dagster-db-backup
  namespace: lotus-lake
spec:
  schedule: "0 0 * * *"
  backupOwnerReference: self
  cluster:
    name: dagster-db
